# 运维分析顾问（AI Agent）设计方案

> 配套调研文档：`AI Agent 调研文档.md`
> 状态：方案设计（待确认后开工）

---

## 0. 一句话定位

**"系统体检医生"**：定期自动给系统做体检 → 发现问题用大白话写报告 → 附上"人话解释 + 原因猜测 + 建议动作" → **人处理后可反馈，反馈回流优化 agent**——形成"感知 → 分析 → 建议 → 人执行 → 反馈 → 优化"的闭环。

---

## 1. 流程总览（五步闭环）

```
① 算数（L0 规则）        ② 判定（L0 规则）        ③ 解读（L1 LLM）
统计 oper_log / login_log   对照健康标准，命中      仅对命中信号调用 LLM
/ sys_config / 表增长      → 生成结构化信号          说人话 + 猜原因 + 给建议
        │                       │                       │
        ▼                       ▼                       ▼
④ 送达与处理（L2 人工）     ⑤ 反馈（L2 人工）
报告 + 建议卡                 每条信号/建议可反馈：
人批准/驳回，人执行变更       👍👎 + 标签 + 文字
        │                       │
        └────── 反馈回流 ───────┘
        调检测阈值（半自动）/ 迭代报告提示词（人工）/ 积累案例库（自动）
```

---

## 2. 三层边界模型（核心原则）

| 层 | 处理方式 | 职责 | 成本 |
|---|---|---|---|
| **L0 规则层** | 确定性代码/SQL | 采集、统计、判定、送达——**只输出事实** | 免费 |
| **L1 LLM 层** | 调用模型 | 解读信号、猜根因、给建议——**只输出参考意见** | 按信号数计，极低 |
| **L2 人工层** | 人决策/执行 | 批准/驳回、执行变更、反馈 | 人的时间 |

**铁律**：
- 问题从 L0 开始处理，**能上规则不上 LLM**
- LLM 调用量 = 信号数量，不是数据量（成本控制根本）
- **会改变系统状态的动作，LLM 永不直接执行**，一律升 L2
- LLM 输出必须标注"事实 vs 推测"，防止误导

---

## 3. 首批检测器（L0 规则，均基于现有数据）

### 安全类
| code | 检测器 | 规则示例 |
|---|---|---|
| `login_brute_force` | 登录爆破 | 5 分钟内同一 IP 失败 ≥ 5 次 |
| `lock_storm` | 锁定风暴 | 当日锁定次数 > 7 日均值 × 3 |
| `abnormal_hour_ops` | 异常时段操作 | 23:00-06:00 管理端写操作 |

### 稳定性类
| code | 检测器 | 规则示例 |
|---|---|---|
| `api_error_surge` | 接口错误突增 | 接口失败计数 24h > 7 日均值 × 3 |
| `slow_ops` | 慢操作 | 耗时 > 5s 的操作清单 |
| `api_fail_rate` | 失败率超标 | 失败率 > 3% 且样本 ≥ 50 |

### 健康类
| code | 检测器 | 规则示例 |
|---|---|---|
| `log_table_growth` | 日志表增长 | 按增速外推达到阈值的天数 |
| `audit_gap` | 审计缺口 | 某时段操作日志条数异常为 0 |

**扩展机制**：检测器实现 `Monitor` 接口（code/name/enabled/check），新增检测 = 新增一个类 + `sys_config` 一条开关/阈值配置，框架零改动。

---

## 4. 报告形态（示例）

> **本周系统体检（07-01 ~ 07-07）**
> - ⚠️ 登录接口平均耗时 180ms→207ms（+15%）【事实】；可能原因：SQL 未走索引 / 与夜间清理任务竞争【推测】；建议：检查该接口 SQL 执行计划【建议卡 A】
> - ⚠️ 周三 02:00-03:00 单一 IP 失败 23 次【事实】；可能：口令爆破【推测】；建议：确认后封禁【建议卡 B】
> - ℹ️ oper_log 按当前增速约 47 天后达 500 万行【事实】；建议：提前归档/分区【建议卡 C】
> - ✅ 其余指标正常（失败率/锁定数/慢操作均在基线内）

---

## 5. 建议卡机制（L2 载体）

字段：**问题描述（事实）| 证据（规则层数据，可点击溯源）| 建议动作（LLM 草稿）| 影响与风险（LLM 评估）| 状态：待审 → 已批准 → 已执行 / 已驳回**

- 审批流：管理员在管理端查看 → 批准/驳回 → **执行动作由人工完成**（第一版）
- 批准/驳回/执行记录全审计（复用 @OperLog）

---

## 6. 反馈闭环（本轮新增，让 agent 越用越准）

### 6.1 反馈入口
- 每条**信号**（报告中）和每张**建议卡**都可反馈：👍/👎 + 原因标签 + **一段自由文本（核心反馈方式）**
- 原因标签（快捷标记）：`误报` / `判断错误` / `建议不可行` / `信息有用` / `已按建议处理`
- 自由文本示例："这个检测器把我们内部测试的账号也当成爆破了，应该排除测试账号的 IP 段"；"建议太笼统，能不能直接给出具体的 SQL 和参数"

### 6.2 反馈数据落库
`ai_feedback`：id / target_type(信号|建议卡) / target_id / user_id / rating / reason_tag / comment / created_at
（与 `ai_analysis_log` 关联，可回放该信号当时的所有上下文）

### 6.3 反馈如何优化 agent（四条路径，全部诚实可落地）

| 路径 | 机制 | 自动化程度 |
|---|---|---|
| **① 阈值自适应** | 某检测器连续 N 条"误报"反馈 → 系统提示"阈值可能过严（当前 5 次/5 分钟，建议 8 次）"→ 人确认后改 `sys_config` | 半自动（检测提示，人确认） |
| **② 提示词迭代** | 定期导出"差评"样本（信号 + LLM 输出 + 反馈）→ 人工分析 → 更新报告生成 prompt | 人工（提供差评样本集） |
| **③ 案例库积累** | "好评"样本（已按建议处理且有效）进 few-shot 案例库，后续报告生成时作为示例——agent 越用越懂这套系统 | 自动积累 |
| **④ 反馈分析层（LLM）** | 定期把**自由文本反馈**交给 LLM 分析：聚类同类问题（"多条反馈都指向同一检测器误报"）、提炼改进点（阈值/提示词/新增检测器）、输出"反馈洞察报告"→ 人确认后应用 | 半自动（LLM 分析，人应用） |

**④ 详解**（新增，对应"多一层 LLM 协助优化"）：
- 触发：每周一次（或反馈积压 ≥ N 条时）
- 输入：本周全部自由文本反馈 + 关联的信号/建议卡/标签
- 输出（反馈洞察报告）：
  - 聚类：把散落的反馈归成几类问题（如"登录爆破检测误报最多，共 6 条"）
  - 提炼：每条给出可执行的改进建议（"排除内网 IP 段后再统计""报告里补充证据链接"）
  - 优先级：按反馈数量/影响排序
- 应用：人确认后 → 改检测器规则 / 改报告 prompt / 加新检测器，落到 `ai_feedback_insight` 表留痕
- 成本：每周 1 次 LLM 调用，可忽略

### 6.4 反馈指标进下一期报告
- 反馈率、好评率、误报率（按检测器）——"你的反馈使 XX 检测器误报率从 30% 降到 8%"，闭环可视化

### 6.5 诚实边界
- 反馈是**优化依据**，不是"AI 自动进化"——所有参数变更需人确认
- 不收集敏感内容；反馈关联用户（审计可溯）

---

## 7. 数据表设计（新增）

```sql
ai_monitor_event   -- 信号：code/维度/数值/时间窗/证据JSON/状态(待解读|已解读|已确认|已忽略)
ai_suggestion      -- 建议卡：event_id/内容/影响/风险/状态(待审|已批|已驳回|已执行)/处理人/处理时间
ai_analysis_log    -- 分析日志：类型/信号id/LLM调用/输入输出摘要/token/耗时/成本（traceId 关联）
ai_feedback        -- 反馈：target_type/target_id/user_id/rating/tag/comment(自由文本)/created_at
ai_feedback_insight -- 反馈洞察：分析批次/聚类结果/改进建议/应用状态(待应用|已应用|已忽略)
```

---

## 8. 成本账（诚实预估）

- L0 采集/判定/送达：**0 元**，占 90% 代码量
- L1 解读：每周报告 ≈ 信号数 × 1 次 LLM 调用（正常系统每周 3-10 个信号 → 几分钱）
- 反馈路径③案例库：好评样本自动沉淀，无额外调用

---

## 9. 落地步骤

**第一阶段（先交付，不依赖 LLM API/密钥）**：
1. 5 张表 + `Monitor` 接口 + 调度器（复用分布式锁模式）
2. 首批 6 个检测器（§3）
3. 报告生成（纯规则文本）+ 建议卡审批流（管理端页面）+ 站内通知
4. 反馈入口 + `ai_feedback` 落库（含自由文本）+ 阈值自适应提示

**第二阶段（插入 LLM 解读层，需 LLM API key）**：
5. LLM 解读服务（信号 → 人话/根因/建议，标注事实与推测，SSE 可选）
6. 建议卡由 LLM 起草
7. **反馈分析层（路径④）**：定期分析自由文本反馈 → 反馈洞察报告 → 人确认应用
8. 提示词迭代工作流 + few-shot 案例库接入

**测试**：
- 每个检测器构造数据跑通（阈值边界用例）
- 调度集成测试（多实例只跑一台，复用 LogCleanTask 锁模式）
- 反馈闭环：误报反馈 → 阈值提示 → 确认调整 全链路

---

## 10. 企业级要求（复用框架底座）

- 权限：报告/建议卡/反馈仅管理员角色；@OperLog 记录审批动作
- 审计：AI 调用全进 `ai_analysis_log`（traceId 关联）
- 限流：LLM 调用接口复用 @RateLimit
- 密钥：LLM API key 环境变量
- 可观测：信号/调用/反馈全部可回放
